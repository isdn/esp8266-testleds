#### Addressable LED Strip testing

1. Initialize PlatformIO, for example:

```shell script
platformio init --ide clion --board d1_mini
```

2. Add FastLED lib to PlatformIO:

```shell script
pio lib install "FastLED"
```